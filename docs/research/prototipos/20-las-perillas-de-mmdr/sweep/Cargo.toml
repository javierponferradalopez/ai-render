[package]
name = "sweep"
version = "0.1.0"
edition = "2021"

[dependencies]
mermaid-rs-renderer = { version = "=0.3.1", default-features = false, features = ["png"] }
serde_json = "1"
anyhow = "1"

[profile.release]
opt-level = 2
